Snapshot files for CLI integration tests.

These snapshots are golden JSON outputs produced by running
`symgrep` against small fixture repositories. They are treated as
part of the stable CLI/JSON API surface; update them intentionally
when behavior or schema changes.
